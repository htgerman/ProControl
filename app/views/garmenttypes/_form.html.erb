<%= form_for(@garmenttype) do |f| %>
  <% if @garmenttype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@garmenttype.errors.count, "error") %> prohibited this garmenttype from being saved:</h2>

      <ul>
      <% @garmenttype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
